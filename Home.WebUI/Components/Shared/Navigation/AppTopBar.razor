@* Top navigation bar with back button, title, and optional actions *@

<header class="flex items-center justify-between px-4 py-3 bg-zinc-950 border-b border-zinc-800 shrink-0 min-h-[64px]">
    <div class="flex items-center gap-3 min-w-0">
        @if (ShowBack)
        {
            <button class="p-2 -ml-2 text-zinc-400 hover:text-zinc-50 rounded-lg transition-colors shrink-0"
                    @onclick="GoBack">
                <svg class="h-5 w-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
                </svg>
            </button>
        }
        <div class="min-w-0">
            <h1 class="text-base font-semibold text-zinc-50 truncate">@Title</h1>
            @if (!string.IsNullOrEmpty(Subtitle))
            {
                <p class="text-xs text-zinc-500 truncate">@Subtitle</p>
            }
        </div>
    </div>
    @if (ActionsContent != null)
    {
        <div class="flex items-center gap-2 shrink-0 ml-3">
            @ActionsContent
        </div>
    }
</header>

@code {
    [Parameter] public string Title { get; set; } = "";
    [Parameter] public string? Subtitle { get; set; }
    [Parameter] public bool ShowBack { get; set; } = true;
    [Parameter] public string BackHref { get; set; } = "/home";
    [Parameter] public RenderFragment? ActionsContent { get; set; }

    private void GoBack()
        => NavigationManager.NavigateTo(BackHref);
}
