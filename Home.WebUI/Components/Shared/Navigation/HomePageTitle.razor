@* The browser tab. Blazor renders this through the HeadOutlet in App.razor, so it works
   without touching the DOM directly and keeps updating when the title does. *@

<PageTitle>@this.FullTitle()</PageTitle>
