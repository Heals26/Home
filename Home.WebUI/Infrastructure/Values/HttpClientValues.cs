namespace Home.WebUI.Infrastructure.Values;

public static class HttpClientValues
{

    #region Fields

    /// <summary>
    /// The named HttpClient that carries the API's base address — the one name shared by
    /// registration and every factory call.
    /// </summary>
    public const string ApiClientName = "HomeApi";

    #endregion Fields

}
